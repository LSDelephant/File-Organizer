# File Organizer (Rust)

CLI утиліта, яка організує файли в директорії за їх розширенням.

---

## 🚀 Використання
```bash
# організувати поточну папку
cargo run --release

# організувати конкретну папку
cargo run --release -- /шлях/до/папки
